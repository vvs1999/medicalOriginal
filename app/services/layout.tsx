import { ReactNode } from "react";

export const metadata = {
  title: "Services - AccurusBill",
  description: "Explore AccurusBill's comprehensive medical billing and revenue cycle management services to optimize your practice.",
};

export default function ServicesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}